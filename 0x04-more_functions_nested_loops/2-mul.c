#include "main.h"

/**
 * mul - function multiplies two integers
 *
 * @a: the fist integer to multiply
 * @b: the second intdger to multilpy
 * Return: the result of the multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
